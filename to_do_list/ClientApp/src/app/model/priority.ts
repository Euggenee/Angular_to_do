export class Priority {
  id: number;
  title: string;
  color?: string;

  constructor(id: number, title: string, color: string) {

    id = this.id,
      title = this.title,
      color = this.color
  }
}
